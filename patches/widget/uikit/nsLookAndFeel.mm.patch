diff --git a/widget/uikit/nsLookAndFeel.mm b/widget/uikit/nsLookAndFeel.mm
index f487a519ce44..25caa748bfbe 100644
--- a/widget/uikit/nsLookAndFeel.mm
+++ b/widget/uikit/nsLookAndFeel.mm
@@ -4,20 +4,62 @@
 
 #import <UIKit/UIColor.h>
 #import <UIKit/UIInterface.h>
+#import <UIKit/UIScreen.h>
 
+#include "mozilla/LookAndFeel.h"
+#include "mozilla/widget/ThemeChangeKind.h"
 #include "nsLookAndFeel.h"
 
 #include "gfxFont.h"
 #include "gfxFontConstants.h"
+#include "gfxPlatformMac.h"
 #include "mozilla/FontPropertyTypes.h"
+// PointerCapabilities lives here - see mse_fix_147's docstring.
+// nsStyleConsts.h below reaches the GENERATED ServoStyleConsts.h,
+// which has StyleTextDecorationStyle and not this.
+#include "mozilla/ServoTypes.h"
 #include "nsStyleConsts.h"
 
 using namespace mozilla;
 
+static bool sSystemUsesDarkTheme;
+static bool sSystemUsesDarkThemeInitialized;
+
+static bool GetSystemUsesDarkTheme() {
+  if (!sSystemUsesDarkThemeInitialized) {
+    sSystemUsesDarkThemeInitialized = true;
+    sSystemUsesDarkTheme =
+        [UIScreen mainScreen].traitCollection.userInterfaceStyle ==
+        UIUserInterfaceStyleDark;
+  }
+  return sSystemUsesDarkTheme;
+}
+
+static UITraitCollection* TraitCollectionForScheme(ColorScheme aColorScheme) {
+  const auto style = aColorScheme == ColorScheme::Dark
+                         ? UIUserInterfaceStyleDark
+                         : UIUserInterfaceStyleLight;
+  return [UITraitCollection traitCollectionWithUserInterfaceStyle:style];
+}
+
 nsLookAndFeel::nsLookAndFeel() : mInitialized(false) {}
 
 nsLookAndFeel::~nsLookAndFeel() {}
 
+void nsLookAndFeel::SetSystemUsesDarkTheme(bool aUseDarkTheme) {
+  if (sSystemUsesDarkThemeInitialized &&
+      sSystemUsesDarkTheme == aUseDarkTheme) {
+    return;
+  }
+
+  sSystemUsesDarkThemeInitialized = true;
+  sSystemUsesDarkTheme = aUseDarkTheme;
+
+  // Recompute cached UIKit look-and-feel values when the host view's
+  // trait collection changes so Gecko can follow the current interface style.
+  LookAndFeel::NotifyChangedAllWindows(widget::ThemeChangeKind::StyleAndLayout);
+}
+
 static nscolor GetColorFromUIColor(UIColor* aColor) {
   CGColorRef cgColor = [aColor CGColor];
   CGColorSpaceModel model = CGColorSpaceGetModel(CGColorGetColorSpace(cgColor));
@@ -34,6 +76,11 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
   return 0;
 }
 
+static nscolor GetResolvedColor(UIColor* aColor, ColorScheme aColorScheme) {
+  return GetColorFromUIColor([aColor
+      resolvedColorWithTraitCollection:TraitCollectionForScheme(aColorScheme)]);
+}
+
 void nsLookAndFeel::NativeInit() { EnsureInit(); }
 
 void nsLookAndFeel::RefreshImpl() {
@@ -49,9 +96,13 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
   nsresult res = NS_OK;
 
   switch (aID) {
-    case ColorID::Highlight:
-      aResult = NS_RGB(0xaa, 0xaa, 0xaa);
+    case ColorID::Highlight: {
+      nscolor accent =
+          GetStandinForNativeColor(ColorID::Accentcolor, aColorScheme);
+      aResult =
+          NS_RGBA(NS_GET_R(accent), NS_GET_G(accent), NS_GET_B(accent), 78);
       break;
+    }
     case ColorID::MozMenuhover:
       aResult = NS_RGB(0xee, 0xee, 0xee);
       break;
@@ -92,7 +143,7 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
     case ColorID::Menutext:
     case ColorID::Infotext:
     case ColorID::Windowtext:
-      aResult = mColorDarkText;
+      aResult = GetResolvedColor([UIColor labelColor], aColorScheme);
       break;
     case ColorID::Activecaption:
       aResult = NS_RGB(0xff, 0xff, 0xff);
@@ -101,14 +152,15 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
       aResult = NS_RGB(0x00, 0x00, 0x00);
       break;
     case ColorID::Appworkspace:
-      aResult = NS_RGB(0xFF, 0xFF, 0xFF);
+      aResult = GetResolvedColor([UIColor systemBackgroundColor], aColorScheme);
       break;
     case ColorID::Background:
       aResult = NS_RGB(0x63, 0x63, 0xCE);
       break;
     case ColorID::Buttonface:
     case ColorID::MozButtonhoverface:
-      aResult = NS_RGB(0xF0, 0xF0, 0xF0);
+      aResult = GetResolvedColor([UIColor secondarySystemBackgroundColor],
+                                 aColorScheme);
       break;
     case ColorID::Buttonhighlight:
       aResult = NS_RGB(0xFF, 0xFF, 0xFF);
@@ -117,7 +169,7 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
       aResult = NS_RGB(0xDC, 0xDC, 0xDC);
       break;
     case ColorID::Graytext:
-      aResult = NS_RGB(0x44, 0x44, 0x44);
+      aResult = GetResolvedColor([UIColor secondaryLabelColor], aColorScheme);
       break;
     case ColorID::Inactiveborder:
       aResult = NS_RGB(0xff, 0xff, 0xff);
@@ -147,7 +199,7 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
       aResult = NS_RGB(0xDA, 0xDA, 0xDA);
       break;
     case ColorID::Menu:
-      aResult = NS_RGB(0xff, 0xff, 0xff);
+      aResult = GetResolvedColor([UIColor systemBackgroundColor], aColorScheme);
       break;
     case ColorID::Infobackground:
       aResult = NS_RGB(0xFF, 0xFF, 0xC7);
@@ -158,21 +210,22 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
     case ColorID::Window:
     case ColorID::Field:
     case ColorID::MozCombobox:
-      aResult = NS_RGB(0xff, 0xff, 0xff);
+      aResult = GetResolvedColor([UIColor systemBackgroundColor], aColorScheme);
       break;
     case ColorID::Fieldtext:
     case ColorID::MozComboboxtext:
-      aResult = mColorDarkText;
+      aResult = GetResolvedColor([UIColor labelColor], aColorScheme);
       break;
     case ColorID::MozDialog:
-      aResult = NS_RGB(0xaa, 0xaa, 0xaa);
+      aResult = GetResolvedColor([UIColor secondarySystemBackgroundColor],
+                                 aColorScheme);
       break;
     case ColorID::MozDialogtext:
     case ColorID::MozCellhighlighttext:
     case ColorID::Selecteditemtext:
     case ColorID::MozColheadertext:
     case ColorID::MozColheaderhovertext:
-      aResult = mColorDarkText;
+      aResult = GetResolvedColor([UIColor labelColor], aColorScheme);
       break;
     case ColorID::MozMacFocusring:
       aResult = NS_RGB(0x3F, 0x98, 0xDD);
@@ -196,6 +249,7 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
       break;
     case ColorID::Linktext:
     case ColorID::Activetext:
+    case ColorID::MozAutofillBackground:
     case ColorID::TargetTextBackground:
     case ColorID::TargetTextForeground:
       aResult = GetStandinForNativeColor(aID, aColorScheme);
@@ -258,7 +312,13 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
       aResult = 0;
       break;
     case IntID::ScrollbarFadeBeginDelay:
-      aResult = 450;
+      // 450 is the macOS value, and it assumes a pointer already on
+      // screen that lands precisely. On touch the user has to notice the
+      // overlay bar, move a thumb across to it, and hit a target only a
+      // few points wide - and at 450 + 200 the bar was fully gone 650ms
+      // after scrolling stopped, which is most of why the scrollbar was
+      // impossible to grab.
+      aResult = 1500;
       break;
     case IntID::ScrollbarFadeDuration:
       aResult = 200;
@@ -293,6 +353,23 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
     case IntID::SpellCheckerUnderlineStyle:
       aResult = static_cast<int32_t>(StyleTextDecorationStyle::Dotted);
       break;
+    case IntID::TouchDeviceSupportPresent:
+      aResult = 1;
+      break;
+    // A FINGER, NOT A MOUSE - see mse_fix_147's docstring. Without
+    // these two the switch falls to its default arm, which returns
+    // NS_ERROR_FAILURE, and nsMediaFeatures.cpp's fallback is
+    // Fine | Hover on everything that is not ANDROID. So every page
+    // has been told it is on a desktop while touch events fire
+    // underneath it, and players have been taking their mouse branch:
+    // click-to-toggle on the picture, dismiss on mouseleave, controls
+    // that expect a hover before a click.
+    //
+    // The same answer widget/android/nsLookAndFeel.cpp gives.
+    case IntID::PrimaryPointerCapabilities:
+    case IntID::AllPointerCapabilities:
+      aResult = static_cast<int32_t>(PointerCapabilities::Coarse);
+      break;
     case IntID::ContextMenuOffsetVertical:
     case IntID::ContextMenuOffsetHorizontal:
       aResult = 2;
@@ -301,6 +378,11 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
       aResult = 0;
       res = NS_ERROR_FAILURE;
   }
+
+  if (aID == IntID::SystemUsesDarkTheme) {
+    aResult = GetSystemUsesDarkTheme() ? 1 : 0;
+    return NS_OK;
+  }
   return res;
 }
 
@@ -325,20 +407,10 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
 
 bool nsLookAndFeel::NativeGetFont(FontID aID, nsString& aFontName,
                                   gfxFontStyle& aFontStyle) {
-  // hack for now
-  if (aID == FontID::Caption || aID == FontID::Menu) {
-    aFontStyle.style = FontSlantStyle::NORMAL;
-    aFontStyle.weight = FontWeight::NORMAL;
-    aFontStyle.width = FontWidth::NORMAL;
-    aFontStyle.size = 14;
-    aFontStyle.systemFont = true;
-
-    aFontName.AssignLiteral("sans-serif");
-    return true;
-  }
-
-  // TODO: implement more here?
-  return false;
+  nsAutoCString name;
+  gfxPlatformMac::LookupSystemFont(aID, name, aFontStyle);
+  aFontName.Assign(NS_ConvertUTF8toUTF16(name));
+  return true;
 }
 
 void nsLookAndFeel::EnsureInit() {
@@ -347,7 +419,7 @@ static nscolor GetColorFromUIColor(UIColor* aColor) {
   }
   mInitialized = true;
 
-  mColorDarkText = GetColorFromUIColor([UIColor darkTextColor]);
+  GetSystemUsesDarkTheme();
 
   RecordTelemetry();
 }
