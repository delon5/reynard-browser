diff --git a/gfx/layers/apz/src/AsyncPanZoomController.cpp b/gfx/layers/apz/src/AsyncPanZoomController.cpp
index e0c930d19dde..394a40d53576 100644
--- a/gfx/layers/apz/src/AsyncPanZoomController.cpp
+++ b/gfx/layers/apz/src/AsyncPanZoomController.cpp
@@ -2523,6 +2523,22 @@ SideBits AsyncPanZoomController::ScrollableDirections() const {
     result = mX.ScrollableDirections() | mY.ScrollableDirections();
   }
 
+#ifndef MOZ_WIDGET_UIKIT
+  // Deliberately skipped on this port. A non-zero fixed-layer bottom
+  // margin means different things on the two platforms:
+  //
+  // On Android it means the dynamic toolbar is displaced, so scrolling up
+  // can bring it back and scrolling down can hide it further - "up" and
+  // "down" stay meaningful even at the scroll extremes, which is why
+  // ScrollableDirectionsWithDynamicToolbar adds eTop and eBottom.
+  //
+  // Here the margin means the browser chrome overlays the page and the
+  // compositor holds fixed content above it. Nothing is revealed or
+  // hidden by scrolling, so adding eTop only makes the page claim it can
+  // always scroll upwards. That is never harmless: pull-to-refresh arms
+  // on "top is NOT among the scrollable edges" (WebContentView's
+  // canOverscrollTop), so a permanently-set eTop silently disables it
+  // for every page, with no error anywhere to say why.
   if (IsRootContent()) {
     if (APZCTreeManager* treeManagerLocal = GetApzcTreeManager()) {
       ScreenMargin fixedLayerMargins =
@@ -2533,6 +2549,7 @@ SideBits AsyncPanZoomController::ScrollableDirections() const {
       }
     }
   }
+#endif
 
   return result;
 }
