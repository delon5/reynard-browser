diff --git a/modules/libpref/init/StaticPrefList.yaml b/modules/libpref/init/StaticPrefList.yaml
index 3d33f9916a24..3c1bb4a54a6f 100644
--- a/modules/libpref/init/StaticPrefList.yaml
+++ b/modules/libpref/init/StaticPrefList.yaml
@@ -5884,7 +5884,7 @@
 # document.createEvent("TouchEvent").
 - name: dom.w3c_touch_events.legacy_apis.enabled
   type: bool
-  value: @IS_ANDROID@
+  value: true
   mirror: always
 
 # W3C touch events
@@ -5895,6 +5895,8 @@
   type: int32_t
 #if defined(XP_MACOSX)
   value: 0
+#elif defined(XP_IOS)
+  value: 1
 #else
   value: 2
 #endif
@@ -5969,7 +5971,7 @@
 # on aarch64 (Apple Silicon) MacOS.
 - name: dom.webgpu.enabled
   type: RelaxedAtomicBool
-#if defined(XP_WIN) || defined(XP_MACOSX) && defined(MOZ_AARCH64)
+#if defined(XP_WIN) || defined (XP_IOS) || defined(XP_MACOSX) && defined(MOZ_AARCH64)
   value: true
 #else
   value: @IS_EARLY_BETA_OR_EARLIER@
@@ -5980,7 +5982,7 @@
 # on a platform that supports the GPU process, else true.
 - name: dom.webgpu.allow-in-parent
   type: bool
-#if defined(XP_WIN) || defined(XP_MACOSX) || defined(ANDROID)
+#if defined(XP_WIN) || defined(XP_MACOSX) || defined(ANDROID) || defined(XP_IOS)
   value: false
 #else
   value: true
@@ -5990,7 +5992,7 @@
 # Is support for the Web GPU API enabled on service workers?
 - name: dom.webgpu.service-workers.enabled
   type: RelaxedAtomicBool
-#if defined(XP_WIN) || defined(XP_MACOSX) && defined(MOZ_AARCH64)
+#if defined(XP_WIN) || defined(XP_IOS) || defined(XP_MACOSX) && defined(MOZ_AARCH64)
   value: true
 #else
   value: @IS_EARLY_BETA_OR_EARLIER@
@@ -6017,7 +6019,7 @@
 
 - name: dom.webgpu.allow-present-without-readback
   type: RelaxedAtomicBool
-#if defined(XP_WIN) || defined(XP_MACOSX) || defined(XP_LINUX) && !defined(ANDROID)
+#if defined(XP_WIN) || defined(XP_MACOSX) || defined(XP_IOS) || defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #else
   value: false
@@ -6027,7 +6029,7 @@
 # Whether WebGPU's GPUExternalTexture API is enabled.
 - name: dom.webgpu.external-texture.enabled
   type: bool
-#if defined(XP_WIN) || defined(XP_MACOSX)
+#if defined(XP_WIN) || defined(XP_MACOSX) || defined(XP_IOS)
   value: true
 #else
   value: false
@@ -6554,7 +6556,7 @@
 # or the parent/main process (false)
 - name: extensions.webextensions.remote
   type: RelaxedAtomicBool
-  value: false
+  value: true
   mirror: always
 
 # Whether to expose the MockExtensionAPI test interface in tests.
@@ -7147,6 +7149,21 @@
 # Nb: we ignore this pref on release and beta.
 - name: gfx.blocklist.all
   type: int32_t
   value: 0
   mirror: once
+
+# Allows picture-in-picture to start from a video that is not in
+# fullscreen. See fix_pip_allow_windowed_video.py.
+#
+# NativeLayerRootCA::FindPictureInPictureSource returns nil unless the
+# window is fullscreen, and on iOS that flag is set from
+# nsWindow::EnteredFullScreen - so it means the page called
+# requestFullscreen, not that the window fills the screen.
+#
+# mirror: always because the check runs under the compositor's mutex,
+# where an ordinary pref read would not be thread-safe.
+- name: gfx.pip.allow_windowed_video
+  type: bool
+  value: false
+  mirror: always
 
@@ -7162,6 +7179,8 @@
   type: bool
 #if defined(XP_MACOSX) || defined(XP_LINUX) && !defined(ANDROID)
   value: true
+#elif defined(MOZ_WIDGET_UIKIT)
+  value: true
 #elif defined(MOZ_WIDGET_ANDROID)
   value: true
 #elif defined(XP_WIN)
@@ -7173,7 +7192,7 @@
 
 - name: gfx.canvas.accelerated.allow-in-parent
   type: bool
-#if defined(XP_WIN) || defined(XP_MACOSX) || defined(ANDROID)
+#if defined(XP_WIN) || defined(XP_MACOSX) || defined(ANDROID) || defined(XP_IOS)
   value: false
 #else
   value: true
@@ -7478,6 +7497,8 @@
     type: RelaxedAtomicBool
 #if defined(MOZ_AARCH64) && defined(XP_MACOSX)
     value: true
+#elif defined(XP_IOS)
+    value: true
 #else
     value: false
 #endif
@@ -7723,7 +7744,13 @@
   # backend.
 -   name: gfx.font_rendering.coretext.enabled
     type: RelaxedAtomicBool
+#if defined(XP_IOS)
+  # iOS system fonts may restrict cmap/table access; CoreText
+  # shaping preserves correct system fallback behavior (including emoji).
+    value: true
+#else
     value: false
+#endif
     mirror: always
 #endif
 
@@ -9262,4 +9289,16 @@
+# Whether a CS_DEBUGGED process should try making its own JIT pages
+# writable before trapping into the debugger. See
+# fix_try_mprotect_before_trap.py.
+#
+# Off by default: this is an experiment, and if it works the debugger
+# apparatus around it becomes unnecessary. mirror: always because the
+# check runs on JIT compilation threads.
+- name: javascript.jit.ios_try_mprotect_first
+  type: bool
+  value: false
+  mirror: always
+
 # The JavaScript JIT compilers. These are read once on startup so a browser may
 # need to be restarted if toggling them. In general each subsequent JIT depends
 # on the ones before it being enabled.
 - name: javascript.options.blinterp
@@ -10013,7 +10052,7 @@
 # or executable but never both at the same time. OpenBSD defaults to W^X.
 - name: javascript.options.content_process_write_protect_code
   type: bool
-#if defined(XP_OPENBSD)
+#if defined(XP_IOS) || defined(XP_OPENBSD)
   value: true
 #else
   value: false
@@ -10188,7 +10227,9 @@
 
 - name: layers.gpu-process.enabled
   type: bool
-#if defined(XP_WIN) || defined(MOZ_WIDGET_ANDROID) || defined(XP_MACOSX)
+# Enable GPU process by default on iOS so Rendering launches in a
+# dedicated NSExtension child process.
+#if defined(XP_WIN) || defined(MOZ_WIDGET_ANDROID) || defined(XP_MACOSX) || defined(XP_IOS)
   value: true
 #elif defined(MOZ_X11)
   value: false  # we'd like this to be @IS_NIGHTLY_BUILD@, see Bug 1927058
@@ -10411,7 +10452,7 @@
 
 - name: layout.accessiblecaret.margin-left
   type: float
-  value: -18.5f
+  value: -17.0f
   mirror: always
 
 - name: layout.accessiblecaret.transition-duration
@@ -11958,7 +11999,7 @@
 - name: media.cubeb.sandbox
   type: bool
   mirror: always
-#if defined(XP_LINUX) || defined(XP_WIN) || defined(XP_MACOSX)
+#if defined(XP_LINUX) || defined(XP_WIN) || defined(XP_MACOSX) || defined(XP_IOS)
   value: true
 #else
   value: false
@@ -12363,6 +12404,8 @@
   value: true
 #elif defined(XP_MACOSX)
   value: true
+#elif defined(XP_IOS)
+  value: true
 #elif defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #elif defined(XP_FREEBSD)
@@ -12407,6 +12450,8 @@
   value: true
 #elif defined(XP_MACOSX)
   value: true
+#elif defined(XP_IOS)
+  value: true
 #elif defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #elif defined(XP_FREEBSD)
@@ -12440,6 +12485,8 @@
   value: true
 #elif defined(XP_MACOSX)
   value: true
+#elif defined(XP_IOS)
+  value: true
 #elif defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #elif defined(XP_FREEBSD)
@@ -12459,6 +12506,8 @@
   value: true
 #elif defined(XP_MACOSX)
   value: true
+#elif defined(XP_IOS)
+  value: true
 #elif defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #elif defined(XP_FREEBSD)
@@ -12478,6 +12527,8 @@
   value: true
 #elif defined(XP_MACOSX)
   value: true
+#elif defined(XP_IOS)
+  value: true
 #elif defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #elif defined(XP_FREEBSD)
@@ -12497,6 +12548,8 @@
   value: true
 #elif defined(XP_MACOSX)
   value: true
+#elif defined(XP_IOS)
+  value: true
 #elif defined(XP_LINUX) && !defined(ANDROID)
   value: true
 #elif defined(XP_FREEBSD)
@@ -12523,7 +12576,7 @@
   type: RelaxedAtomicBool
 #if defined(XP_WIN)
   value: true
-#elif defined(XP_MACOSX)
+#elif defined(XP_MACOSX) || defined(XP_IOS)
   value: true
 #elif defined(XP_LINUX) || defined(ANDROID)
   value: true
@@ -12588,7 +12641,7 @@
 #ifdef MOZ_FFMPEG
 -   name: media.ffmpeg.enabled
     type: RelaxedAtomicBool
-  #if defined(XP_MACOSX)
+  #if defined(XP_MACOSX) || defined(XP_IOS)
     value: false
   #else
     value: true
@@ -15074,7 +15127,10 @@
 - name: network.process.enabled
   type: RelaxedAtomicBool
   mirror: always
-#if defined(ANDROID) || defined(MOZ_THUNDERBIRD) || defined(XP_IOS)
+# Enable socket process networking on iOS.
+#if defined(XP_IOS)
+  value: true
+#elif defined(ANDROID) || defined(MOZ_THUNDERBIRD)
   value: false # see bug 1641427
 #else
   value: true
@@ -15123,7 +15179,13 @@
 - name: network.http.network_access_on_socket_process.enabled
   type: RelaxedAtomicBool
   mirror: always
+  #  value: false
+# Enable socket process networking on iOS.
+#if defined(XP_IOS)
+  value: true
+#else
   value: false
+#endif
 
 # Telemetry of traffic categories. Whether or not to enable HttpTrafficAnalyzer.
 - name: network.traffic_analyzer.enabled
@@ -19129,7 +19191,7 @@
 # If the service is available, set threads to low-power mode when in the background.
 - name: threads.use_low_power.enabled
   type: RelaxedAtomicBool
-#ifdef XP_MACOSX
+#if defined(XP_MACOSX) || defined(XP_IOS)
   value: true
 #else
   value: @IS_NIGHTLY_BUILD@
@@ -19138,10 +19200,10 @@
 
 
 # If the process priority is set to background, put the main thread in the background.
-# Currently off by default.
+# Enabled by default where low-power thread QoS is supported.
 - name: threads.lower_mainthread_priority_in_background.enabled
   type: bool
-#ifdef XP_MACOSX
+#if defined(XP_MACOSX) || defined(XP_IOS)
   value: true
 #else
   value: @IS_NIGHTLY_BUILD@
@@ -19938,7 +20000,7 @@
 # on a platform that supports the GPU process, else true.
 - name: webgl.allow-in-parent
   type: bool
-#if defined(XP_WIN) || defined(XP_MACOSX) || defined(ANDROID)
+#if defined(XP_WIN) || defined(XP_MACOSX) || defined(ANDROID) || defined(XP_IOS)
   value: false
 #else
   value: true
