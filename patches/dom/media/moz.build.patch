diff --git a/dom/media/moz.build b/dom/media/moz.build
index 28bbbf6..aa32007 100644
--- a/dom/media/moz.build
+++ b/dom/media/moz.build
@@ -25,6 +25,9 @@ component_av = ("Core", "WebRTC: Audio/Video")
 with Files("GetUserMedia*"):
     BUG_COMPONENT = component_av
 
+if CONFIG["MOZ_WEBRTC"]:
+    DIRS += ["bridge"]
+
 DIRS += [
     "autoplay",
     "doctor",
@@ -60,8 +63,11 @@ DIRS += [
 if CONFIG["MOZ_ANDROID_HLS_SUPPORT"]:
     DIRS += ["hls"]
 
-if CONFIG["MOZ_WEBRTC"]:
-    DIRS += ["bridge"]
+if CONFIG["MOZ_WIDGET_TOOLKIT"] == "uikit":
+    # Objective-C++, so it cannot join the unified C++ translation units.
+    SOURCES += [
+        "AVPlayerDecoder.mm",
+    ]
 
 if CONFIG["MOZ_WEBSPEECH"]:
     DIRS += ["webspeech"]
@@ -152,6 +158,8 @@ EXPORTS += [
     "AudioSampleFormat.h",
     "AudioSegment.h",
     "AudioStream.h",
+    "AVPlayerDecoder.h",
+    "AVPlayerDemuxer.h",
     "BackgroundVideoDecodingPermissionObserver.h",
     "BitReader.h",
     "BitWriter.h",
@@ -279,6 +287,7 @@ UNIFIED_SOURCES += [
     "AudioStreamTrack.cpp",
     "AudioTrack.cpp",
     "AudioTrackList.cpp",
+    "AVPlayerDemuxer.cpp",
     "BackgroundVideoDecodingPermissionObserver.cpp",
     "BaseMediaResource.cpp",
     "BitReader.cpp",
