diff --git a/dom/ipc/VsyncParent.h b/dom/ipc/VsyncParent.h
index 9336a88..5271fbf 100644
--- a/dom/ipc/VsyncParent.h
+++ b/dom/ipc/VsyncParent.h
@@ -43,6 +43,10 @@ class VsyncParent final : public PVsyncParent, public VsyncObserver {

   bool mObservingVsync;
   bool mDestroyed;
+  // vsyncTrace diagnostic only: cleared on every RecvObserve, set when
+  // the first tick of that observe cycle is sent to the child, so
+  // DispatchVsyncEvent can log once per cycle instead of at frame rate.
+  bool mLoggedTickAfterObserve = false;
   nsCOMPtr<nsIThread> mInitialThread;
   RefPtr<VsyncDispatcher> mVsyncDispatcher;
 };
