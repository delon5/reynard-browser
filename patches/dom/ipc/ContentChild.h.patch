diff --git a/dom/ipc/ContentChild.h b/dom/ipc/ContentChild.h
index 8c9896a71f94..64f17ef11c14 100644
--- a/dom/ipc/ContentChild.h
+++ b/dom/ipc/ContentChild.h
@@ -191,17 +191,50 @@ class ContentChild final : public PContentChild,
       Endpoint<PImageBridgeChild>&& aImageBridge,
       Endpoint<PVRManagerChild>&& aVRBridge,
       Endpoint<PRemoteMediaManagerChild>&& aVideoManager,
-      nsTArray<uint32_t>&& namespaces);
+      nsTArray<uint32_t>&& namespaces, const uint64_t& aProcessToken);
 
   mozilla::ipc::IPCResult RecvReinitRendering(
       Endpoint<PCompositorManagerChild>&& aCompositor,
       Endpoint<PImageBridgeChild>&& aImageBridge,
       Endpoint<PVRManagerChild>&& aVRBridge,
       Endpoint<PRemoteMediaManagerChild>&& aVideoManager,
-      nsTArray<uint32_t>&& namespaces);
+      nsTArray<uint32_t>&& namespaces, const uint64_t& aProcessToken);
 
   mozilla::ipc::IPCResult RecvReinitRenderingForDeviceReset();
 
+  // Reynard: reports from the AVPlayer this process asked the app
+  // process to own. See PContent.ipdl's AVPlayerCreate.
+  mozilla::ipc::IPCResult RecvAVPlayerMetadata(const uint64_t& aPlayerId,
+                                               const double& aDurationSeconds,
+                                               const int32_t& aWidth,
+                                               const int32_t& aHeight);
+  mozilla::ipc::IPCResult RecvAVPlayerTime(const uint64_t& aPlayerId,
+                                           const double& aSeconds);
+  mozilla::ipc::IPCResult RecvAVPlayerEnded(const uint64_t& aPlayerId);
+  mozilla::ipc::IPCResult RecvAVPlayerError(const uint64_t& aPlayerId);
+  mozilla::ipc::IPCResult RecvAVPlayerProtected(const uint64_t& aPlayerId);
+  // Reynard AirPlay: what the app process saw of the player - external
+  // playback, or a pause/play the page never asked for. See
+  // PContent.ipdl's AVPlayerHostState for the kinds.
+  mozilla::ipc::IPCResult RecvAVPlayerHostState(const uint64_t& aPlayerId,
+                                                const uint32_t& aKind,
+                                                const bool& aValue);
+  // Reynard FairPlay: the AVContentKeySession is the app process's, so
+  // both halves of the licence conversation that face the PAGE arrive
+  // here. See PContent.ipdl.
+  mozilla::ipc::IPCResult RecvAVPlayerKeyEncrypted(
+      const uint64_t& aPlayerId, const nsCString& aInitDataType,
+      nsTArray<uint8_t>&& aInitData);
+  mozilla::ipc::IPCResult RecvAVPlayerKeyMessage(const uint64_t& aPlayerId,
+                                                 const nsCString& aContentId,
+                                                 nsTArray<uint8_t>&& aSpc);
+  mozilla::ipc::IPCResult RecvAVPlayerFrame(const uint64_t& aPlayerId,
+                                            const uint32_t& aSurfaceId);
+  mozilla::ipc::IPCResult RecvAVPlayerFrameData(
+      const uint64_t& aPlayerId, mozilla::ipc::Shmem&& aPixels,
+      const int32_t& aWidth, const int32_t& aHeight,
+      const uint32_t& aYStride, const uint32_t& aCbCrStride);
+
   mozilla::ipc::IPCResult RecvSetProcessSandbox(
       const Maybe<FileDescriptor>& aBroker);
 
